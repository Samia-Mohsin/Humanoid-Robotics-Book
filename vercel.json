{
  "version": 2,
  "builds": [
    {
      "src": "unified-site/package.json",
      "use": "@vercel/static-build",
      "config": {
        "rootDirectory": "unified-site",
        "buildCommand": "npm run build",
        "outputDirectory": "build"
      }
    }
  ]
}